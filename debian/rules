#!/usr/bin/make -f

%:
	dh $@

override_dh_installsystemd:
	dh_installsystemd -padaptation-starqlte-configs --name=batman batman.service --no-start
	dh_installsystemd -padaptation-starqlte-configs --name=audiohal audiohal.service --no-start
